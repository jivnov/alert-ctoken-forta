import Web3 from "web3";
export default class AgentRegistry {
    private contract;
    constructor(web3AgentRegistry: Web3, agentRegistryContractAddress: string);
    agentExists(agentId: string): Promise<boolean>;
    createAgent(from: string, agentId: string, reference: string): Promise<void>;
    updateAgent(from: string, agentId: string, reference: string): Promise<void>;
    isEnabled(agentId: string): Promise<any>;
    disableAgent(from: string, agentId: string): Promise<void>;
    enableAgent(from: string, agentId: string): Promise<void>;
}
